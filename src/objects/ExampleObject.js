class ExampleObject {

  constructor(game){
    // TODOs for tomorrow

    // Fix jump to consistent height
    // remove magic numbers
    // Fade screen to black
    // Look into putting the levels in states rather than objects
    // Make level 2! 
  }

}

export default ExampleObject;